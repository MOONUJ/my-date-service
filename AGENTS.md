# AGENTS.md

## 프로젝트 개요

이 저장소는 개인용 AI 데이트 장소·맛집 플래너를 만든다.

핵심 가치는 포털/지도 검색 결과에 사용자의 자연어 취향과 이동 수단을 결합해 다음 정보를 한 화면에서 제공하는 것이다.

- 개인 취향에 맞춘 추천 TOP 3
- 각 추천의 짧고 검증 가능한 이유
- 자차 이용자를 위한 주차 정보 또는 대중교통 이용자를 위한 역·출구·도보 정보
- 지도 핀과 전체 장소 검색 결과

제품 요구사항의 기준 문서는 `.planing/프로젝트_서비스_정의.md`, `.planing/프로젝트_핵심기능.md`, `.planing/화면_예시_01.html`이다. 구현과 문서가 충돌하면 임의로 요구사항을 바꾸지 말고 이 문서들을 먼저 확인한다. `.planing`은 현재 저장소에서 사용 중인 의도된 디렉터리명이므로 임의로 `.planning`으로 바꾸지 않는다.

## 제품 원칙

우선순위는 다음과 같다.

1. 개인이 실제로 반복해서 쓸 수 있는 간결한 흐름
2. Cloudflare 무료 티어 안에서 가능한 운영비 최소화
3. 모바일에서 완전하게 사용할 수 있는 반응형 UI
4. 추천 근거와 원본 데이터 출처를 구분해 보여주는 신뢰성
5. 과도한 기능, 인프라, AI 호출을 피하는 작은 MVP

초기 MVP 범위는 회원가입/로그인, 취향 프롬프트 저장·수정, 장소 검색, AI TOP 3와 추천 이유, 교통·주차 정보, 지도/목록 통합 화면이다. 저장한 장소, 복잡한 코스 자동 생성, 소셜 기능, 관리자 화면 등은 핵심 흐름이 검증된 뒤에 추가한다.

계획 문서의 “1초 만에 완성”과 “탐색 시간 90% 단축”은 제품 목표이지 검증된 성능 보장이 아니다. 측정 전에는 UI나 문서에서 확정적 보장으로 표현하지 않는다.

## 기본 기술·배포 방향

새 기술을 추가해야 할 때는 다음 기본안을 우선 검토한다.

- 프런트엔드와 API는 하나의 TypeScript 프로젝트로 유지한다.
- Cloudflare Workers의 정적 자산 제공과 Worker API를 우선한다. 프레임워크가 필요하면 Cloudflare에서 공식 지원되고 번들이 작은 구성을 선택한다.
- 관계형 데이터는 D1을 우선 검토한다. 사용자, 취향, 검색/추천 메타데이터처럼 구조화된 최소 데이터만 저장한다.
- 이미지·대용량 파일이 실제로 필요해질 때만 R2를 도입한다.
- KV, Durable Objects, Queues, Vectorize, Workers AI 등은 명확한 요구와 무료 티어 적합성이 확인되기 전에는 추가하지 않는다.
- 배포와 로컬 개발은 Wrangler 설정을 저장소에서 버전 관리한다.
- 외부 비밀값은 Wrangler secrets/bindings로 주입하고 저장소, 클라이언트 번들, 로그에 넣지 않는다.
- Cloudflare 제품의 가격·무료 한도·API·설정은 자주 바뀔 수 있으므로 도입 직전에 공식 문서에서 최신 내용을 확인한다. 무료 한도를 넘기거나 유료 기능을 활성화할 가능성이 있으면 구현 전에 사용자에게 비용 영향을 알린다.
- 자동 확장만 믿고 무제한 요청을 허용하지 않는다. 인증 사용자 단위 rate limit, 입력 길이 제한, 타임아웃, 외부 API 실패 시의 안전한 fallback을 설계한다.

패키지나 별도 서비스는 “이미 있는 기능으로 해결할 수 없는가?”, “지속 비용이나 공급자 종속이 생기는가?”, “무료 티어에서 개인 사용량을 감당하는가?”를 확인한 뒤 추가한다.

## 비용을 낮추는 데이터·AI 원칙

- 검색할 때마다 전체 결과를 LLM에 그대로 보내지 않는다. 지도 API의 구조화 필드로 먼저 필터링하고 작은 후보군만 평가한다.
- 가능한 점수 계산은 결정론적 규칙으로 처리하고, LLM은 최종 후보의 순위 보정과 짧은 추천 사유 생성에 집중시킨다.
- 취향 정규화 결과와 동일 검색의 추천 결과는 개인정보 및 최신성 요구를 고려해 캐시한다. 캐시 키에는 사용자/취향 버전, 검색 조건, 이동 수단, 모델/프롬프트 버전을 포함한다.
- 저비용 모델과 짧은 구조화 출력(JSON)을 기본으로 하며, 출력 토큰과 후보 수에 상한을 둔다.
- LLM이 장소의 주차, 영업시간, 거리 같은 사실을 만들어내게 하지 않는다. 지도/장소 API의 사실과 AI의 해석을 구분한다. 근거 데이터가 없으면 “정보 없음”으로 표시한다.
- 지도 공급자는 한국 지역의 정확도, 허용된 표시/캐싱 범위, 무료 호출량, 지도 SDK와 장소 검색 API의 약관을 비교한 뒤 하나를 선택한다. 여러 공급자를 동시에 붙이는 것은 MVP 이후로 미룬다.
- OpenAI와 지도 API 키는 서버에서만 사용한다. 외부 API 호출 수, 실패율, 지연 시간과 AI 토큰 사용량을 개인정보 없이 관찰 가능하게 만든다.
- 검색 입력에 debounce, 명시적 검색 실행, 중복 요청 취소를 적용해 지도 및 AI API의 불필요한 호출을 줄인다.

## 인증과 개인정보

- 개인용 서비스라도 인증·인가 경계를 생략하지 않는다. 모든 사용자 데이터 쿼리는 인증된 사용자 ID로 범위를 제한한다.
- JWT를 직접 구현해야 한다면 강한 해시 기반 비밀번호 저장, 짧은 수명의 세션/토큰, 안전한 갱신·폐기, `HttpOnly`, `Secure`, `SameSite` 쿠키를 우선 검토한다. 토큰을 `localStorage`에 두는 설계를 기본값으로 삼지 않는다.
- 선호 프롬프트는 개인 성향 데이터로 취급한다. 필요한 데이터만 저장하고 로그, 오류 추적, 분석 이벤트에 원문을 남기지 않는다.
- 계정 삭제 시 사용자 데이터 삭제 경로를 제공한다.
- AI 프롬프트와 장소 데이터는 신뢰할 수 없는 입력이다. 길이와 형식을 검증하고, 구조화 출력 스키마를 검증하며, 모델 출력은 HTML로 직접 삽입하지 않는다.
- 지도/AI 제공자에 전달되는 정보와 개인정보 처리 방침이 실제 구현과 일치해야 한다.

## 모바일 우선 UI

`.planing/화면_예시_01.html`은 시각적 방향을 보여주는 데스크톱 예시이지 그대로 복사할 완성 구현이 아니다. CDN Tailwind, FontAwesome CDN, Google Static Maps 더미 배경과 하드코딩된 사용자/장소 정보는 프로덕션 의존성이 아니다.

- 가장 작은 지원 폭부터 설계하고 큰 화면으로 확장한다. 320px에서도 가로 스크롤 없이 핵심 흐름이 동작해야 한다.
- 모바일에서는 지도와 384px 고정 사이드바를 나란히 두지 않는다. 지도/목록 전환, 하단 시트, 또는 접을 수 있는 카드 흐름을 사용한다.
- 검색, 이동 수단, TOP 3, 전체 결과, 상세 정보에 엄지손가락으로 도달하기 쉬운 순서를 유지한다.
- 터치 대상은 충분히 크게 하고 hover에만 의존하지 않는다. 키보드 탐색, 보이는 focus, 의미 있는 label, 색상 대비, 화면 읽기 순서를 보장한다.
- `100vh` 고정 레이아웃 대신 모바일 브라우저의 동적 뷰포트와 safe-area를 고려한다.
- 지도만으로 정보를 전달하지 않고 동일한 장소 목록을 제공한다. 지도 SDK 실패, 위치 권한 거부, 느린 네트워크에도 검색 결과를 읽을 수 있어야 한다.
- loading, empty, partial result, stale cache, rate-limit, external API failure 상태를 명시적으로 디자인한다.
- 실제 모바일 뷰포트와 데스크톱 뷰포트 모두에서 시각적으로 확인한다.

## 구현 규칙

- TypeScript의 strict 설정을 유지하고 `any`를 임시 해결책으로 남기지 않는다.
- 외부 서비스 응답은 런타임 스키마로 검증하고 내부 도메인 타입으로 변환한다.
- 장소 검색, 추천 점수화, AI 설명 생성, 교통 정보 조회를 분리해 각 단계가 독립적으로 실패하거나 캐시될 수 있게 한다.
- API 응답은 안정적인 오류 코드와 사용자에게 안전한 메시지를 사용한다. 내부 오류, 비밀값, 전체 외부 응답을 노출하지 않는다.
- 접근성 있는 HTML을 우선하고, UI 문자열은 한국어를 기본으로 한다.
- 새 의존성은 필요성과 번들/보안/유지보수 비용을 설명할 수 있을 때만 추가한다.
- 데이터베이스 변경은 재현 가능한 migration으로 관리한다.
- 기능 변경에는 정상 흐름뿐 아니라 인증 실패, 빈 결과, 잘못된 외부 응답, 제한 초과를 다루는 테스트를 추가한다.

## 작업 및 검증 절차

작업 전 관련 `.planing` 문서, 현재 코드, 가까운 테스트와 설정을 읽는다. 변경은 요구사항을 만족하는 최소 단위로 만들고 unrelated refactor를 섞지 않는다.

복잡하거나 여러 파일에 걸친 개발 작업에는 저장소 스킬 `.agents/skills/development-loop/SKILL.md`를 사용한다. 기본 루프는 목표와 완료 조건 정의 → 저장소 조사 → 실행 가능한 계획 → 작은 단위 구현 → 자동 검증 → 실제 동작 확인 → diff 자체 검토 → 필요한 수정과 재검증 → 커밋·푸시이다. 단순한 문구 수정처럼 위험이 낮은 작업에는 형식적인 계획을 강제하지 않되 검증과 diff 확인은 생략하지 않는다.

작업 요청은 가능하면 다음 네 요소로 해석한다.

- Goal: 만들어야 하는 결과
- Context: 관련 요구사항, 파일, 예시, 오류
- Constraints: 비용, 보안, 호환성, 범위 제한
- Done when: 테스트, 동작, 수치 또는 리뷰 기준으로 확인할 완료 조건

요청이 모호하면서 결과를 크게 바꿀 수 있는 선택만 사용자에게 확인한다. 저장소와 문서에서 답을 찾을 수 있는 세부사항은 조사 후 합리적인 기본값으로 진행한다. 독립적인 작업은 별도 작업 트리 또는 충돌하지 않는 범위에서만 병렬화하고, 같은 파일을 여러 에이전트가 동시에 수정하지 않는다.

복잡하거나 여러 파일에 걸친 개발 작업에는 저장소 스킬 `.agents/skills/development-loop/SKILL.md`를 사용한다. 기본 루프는 목표와 완료 조건 정의 → 저장소 조사 → 실행 가능한 계획 → 작은 단위 구현 → 자동 검증 → 실제 동작 확인 → diff 자체 검토 → 필요한 수정과 재검증 → 커밋·푸시이다. 단순한 문구 수정처럼 위험이 낮은 작업에는 형식적인 계획을 강제하지 않되 검증과 diff 확인은 생략하지 않는다.

작업 요청은 가능하면 다음 네 요소로 해석한다.

- Goal: 만들어야 하는 결과
- Context: 관련 요구사항, 파일, 예시, 오류
- Constraints: 비용, 보안, 호환성, 범위 제한
- Done when: 테스트, 동작, 수치 또는 리뷰 기준으로 확인할 완료 조건

요청이 모호하면서 결과를 크게 바꿀 수 있는 선택만 사용자에게 확인한다. 저장소와 문서에서 답을 찾을 수 있는 세부사항은 조사 후 합리적인 기본값으로 진행한다. 독립적인 작업은 별도 작업 트리 또는 충돌하지 않는 범위에서만 병렬화하고, 같은 파일을 여러 에이전트가 동시에 수정하지 않는다.

완료 전 가능한 범위에서 다음을 확인한다.

- format, lint, typecheck, 단위/통합 테스트
- Worker와 D1을 사용하는 경우 로컬 Wrangler 환경에서 API와 migration
- 320px 전후 모바일, 일반 모바일, 데스크톱 레이아웃
- 키보드 탐색, focus, label, 대비와 reduced motion
- 외부 API 오류, 느린 응답, 빈 결과, rate limit, 비로그인 상태
- 클라이언트 번들·커밋·로그에 비밀값이나 개인정보가 없는지
- Cloudflare 무료 티어와 외부 API 비용에 영향을 주는 변경인지

실행하지 못한 검증과 남은 비용/보안 위험은 결과 보고에 명시한다.

### Git과 GitHub 전달

- 작업 시작 시 현재 브랜치, remote, `git status`를 확인한다. 기존 변경은 사용자 소유로 간주하고 덮어쓰거나 임의로 커밋하지 않는다.
- 기능 하나 또는 수정 하나가 검증된 상태를 하나의 응집된 커밋으로 만든다. Conventional Commits 형식의 간결한 메시지(예: `feat: add mobile search layout`)를 사용한다.
- 커밋 전 diff를 다시 읽고 생성물, 임시 파일, 비밀값, 개인정보, 무관한 변경이 포함되지 않았는지 확인한다. 관련 검사에 실패한 상태를 정상 커밋으로 포장하지 않는다.
- 개발을 요청받은 작업에서는 검증이 끝난 에이전트 소유 변경을 커밋하고 현재 브랜치의 추적 remote로 푸시하는 것을 기본 전달 단계로 삼는다. remote/업스트림이 없거나 인증·보호 규칙·충돌로 실패하면 추측으로 우회하지 말고 로컬 커밋과 실패 원인을 보고한다.
- `main`/`master` 직접 푸시가 저장소 관행과 충돌하거나 변경 위험이 크면 `codex/` 접두사의 작업 브랜치를 사용한다. force push, history rewrite, 기존 커밋 수정, 태그/릴리스 생성은 사용자가 명시적으로 요청하지 않으면 하지 않는다.
- 푸시 직전에 원격 대상과 포함 커밋을 확인한다. 사용자가 명시한 범위를 넘어 PR 생성, merge, issue 변경, release 또는 배포까지 수행하지 않는다.
- GitHub 플러그인/커넥터가 실제로 연결되어 있고 권한이 확인된 경우 PR·issue·checks 같은 GitHub 원격 객체를 읽거나 요청된 작업을 수행하는 데 사용한다. 로컬 diff, commit, branch 작업은 Git CLI를 우선한다. 연결 여부를 확인하지 못했으면 설치·인증됐다고 가정하지 않는다.
- GitHub의 PR 생성, 리뷰 제출, merge, issue 작성처럼 다른 사람이나 원격 상태에 영향을 주는 작업은 해당 작업에 대한 사용자 요청이 있을 때만 수행하고, 완료 후 URL과 결과를 보고한다.

### Git과 GitHub 전달

- 작업 시작 시 현재 브랜치, remote, `git status`를 확인한다. 기존 변경은 사용자 소유로 간주하고 덮어쓰거나 임의로 커밋하지 않는다.
- 기능 하나 또는 수정 하나가 검증된 상태를 하나의 응집된 커밋으로 만든다. Conventional Commits 형식의 간결한 메시지(예: `feat: add mobile search layout`)를 사용한다.
- 커밋 전 diff를 다시 읽고 생성물, 임시 파일, 비밀값, 개인정보, 무관한 변경이 포함되지 않았는지 확인한다. 관련 검사에 실패한 상태를 정상 커밋으로 포장하지 않는다.
- 개발을 요청받은 작업에서는 검증이 끝난 에이전트 소유 변경을 커밋하고 현재 브랜치의 추적 remote로 푸시하는 것을 기본 전달 단계로 삼는다. remote/업스트림이 없거나 인증·보호 규칙·충돌로 실패하면 추측으로 우회하지 말고 로컬 커밋과 실패 원인을 보고한다.
- `main`/`master` 직접 푸시가 저장소 관행과 충돌하거나 변경 위험이 크면 `codex/` 접두사의 작업 브랜치를 사용한다. force push, history rewrite, 기존 커밋 수정, 태그/릴리스 생성은 사용자가 명시적으로 요청하지 않으면 하지 않는다.
- 푸시 직전에 원격 대상과 포함 커밋을 확인한다. 사용자가 명시한 범위를 넘어 PR 생성, merge, issue 변경, release 또는 배포까지 수행하지 않는다.
- GitHub 플러그인/커넥터가 실제로 연결되어 있고 권한이 확인된 경우 PR·issue·checks 같은 GitHub 원격 객체를 읽거나 요청된 작업을 수행하는 데 사용한다. 로컬 diff, commit, branch 작업은 Git CLI를 우선한다. 연결 여부를 확인하지 못했으면 설치·인증됐다고 가정하지 않는다.
- GitHub의 PR 생성, 리뷰 제출, merge, issue 작성처럼 다른 사람이나 원격 상태에 영향을 주는 작업은 해당 작업에 대한 사용자 요청이 있을 때만 수행하고, 완료 후 URL과 결과를 보고한다.

## 스킬·플러그인 사용 기준

현재 세션에 설치된 기능 중 이 프로젝트에 관련 있는 것만 목적에 맞게 사용한다. 스킬은 이름이 비슷하다는 이유만으로 모두 불러오지 않는다.

- `cloudflare:cloudflare`: Cloudflare 제품 선택, 아키텍처, 저장소, 네트워크 또는 배포 전반을 다룰 때 사용한다.
- `cloudflare:workers-best-practices`: Worker 코드나 Wrangler 설정을 작성·검토할 때 사용한다.
- `cloudflare:wrangler`: Wrangler 명령을 실행하거나 설정·배포·binding·secret을 다룰 때 먼저 사용한다.
- `cloudflare:durable-objects`: 실시간 협업이나 강한 일관성의 상태 조정이 실제 요구될 때만 사용한다. 현재 MVP의 기본 선택은 아니다.
- `cloudflare:web-perf`: 구현된 페이지의 성능, Core Web Vitals, 로딩 병목을 감사할 때 사용한다.
- `openai-docs`: OpenAI API, 모델 선택, 구조화 출력, SDK, 가격 등 최신 OpenAI 제품 정보가 필요할 때 사용한다.
- `.agents/skills/development-loop`: 여러 단계의 기능 개발, 리팩터링, 버그 수정처럼 계획·구현·검증·Git 전달을 하나의 반복 가능한 흐름으로 수행할 때 사용한다.
- `.agents/skills/development-loop`: 여러 단계의 기능 개발, 리팩터링, 버그 수정처럼 계획·구현·검증·Git 전달을 하나의 반복 가능한 흐름으로 수행할 때 사용한다.
- `product-design:index` 및 하위 스킬: 사용자가 제품 디자인을 명시적으로 요청했거나, 흐름 감사·시각 탐색·참조 화면 구현이 주된 작업일 때 사용한다. 일반 기능 구현에 자동 적용하지 않는다.
- `browser:control-in-app-browser`: 로컬 웹 앱의 반응형 동작, 실제 상호작용, 접근성 상태를 브라우저에서 확인할 때 사용한다.
- `codex-security:security-diff-scan`: PR, 커밋, 브랜치 또는 작업 트리 diff의 보안 검토를 요청받았을 때 사용한다. 전체 저장소 감사에는 해당하는 repository scan 스킬을 사용한다.
- `sites:sites-building`/`sites:sites-hosting`: 이 프로젝트의 기본 배포 대상은 Cloudflare이므로 사용하지 않는다. 사용자가 명시적으로 OpenAI Sites로 전환하라고 요청하거나 `.openai/hosting.json`이 생긴 경우에만 해당 지침을 따른다.
- 문서, PDF, 프레젠테이션, 스프레드시트, 이미지 생성 스킬은 그런 산출물을 명시적으로 만들거나 편집할 때만 사용한다.

추천 목록에만 있고 설치되지 않은 플러그인은 사용할 수 있다고 가정하지 않는다. Airtable, Todoist, Google Calendar, Figma, Gmail, Google Drive 등 외부 서비스 연동은 사용자가 그 서비스를 명시적으로 요청하고 현재 도구로 접근할 수 없을 때에만 설치를 제안한다. 핵심 서비스 구현을 위해 불필요한 플러그인 설치나 외부 SaaS 의존성을 추가하지 않는다.

반복 작업이 두 번 이상 나타나거나 동일한 실수가 반복되고, 저장소 고유의 판단 기준이 필요할 때만 새 스킬 후보로 본다. 새 스킬은 한 가지 작업에 집중하고 실제 입력·출력·완료 조건이 명확해야 한다. 프로젝트 공유 스킬은 `.agents/skills/<skill-name>/SKILL.md`에 두고 `skill-creator`로 생성·검증한다. 단발성 규칙은 현재 요청이나 AGENTS.md에 두며, 외부 서비스 연결이 필요한 경우에만 플러그인/MCP를 고려한다. 스킬을 만들었다는 이유만으로 배포, 푸시, 외부 쓰기 권한이 확대되지는 않는다.

반복 작업이 두 번 이상 나타나거나 동일한 실수가 반복되고, 저장소 고유의 판단 기준이 필요할 때만 새 스킬 후보로 본다. 새 스킬은 한 가지 작업에 집중하고 실제 입력·출력·완료 조건이 명확해야 한다. 프로젝트 공유 스킬은 `.agents/skills/<skill-name>/SKILL.md`에 두고 `skill-creator`로 생성·검증한다. 단발성 규칙은 현재 요청이나 AGENTS.md에 두며, 외부 서비스 연결이 필요한 경우에만 플러그인/MCP를 고려한다. 스킬을 만들었다는 이유만으로 배포, 푸시, 외부 쓰기 권한이 확대되지는 않는다.

## 의사결정이 필요한 경우

다음 결정은 코드에 암묵적으로 고정하기 전에 사용자에게 비용과 선택지를 알린다.

- 카카오·네이버·Google 중 지도/장소 공급자 확정
- OpenAI 유료 호출 예산과 허용 가능한 월간 상한
- 공개 서비스 여부와 회원가입 허용 범위
- 유료 Cloudflare 기능 또는 무료 한도 초과 가능성이 있는 기능
- 장소/추천 기록의 보존 기간과 계정 삭제 정책

그 외의 세부 구현은 이 문서의 개인용, 모바일 우선, 무료 티어 우선 원칙에 맞춰 합리적인 기본값으로 진행한다.
